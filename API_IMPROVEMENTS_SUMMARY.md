# Lưu trữ hiện đại hóa API / API modernization archive

## Tiếng Việt

Bản tóm tắt triển khai năm 2025 ban đầu đề cập đến các module đã bị loại bỏ, frontend Next.js dự kiến và số liệu kiểm thử/coverage lịch sử. Tài liệu này được giữ lại dưới dạng ghi chú ngắn để các thông tin đó không bị hiểu là trạng thái hiện tại của dự án.

API đang hoạt động là v2 với khóa JSON tiếng Anh. Xem thiết lập, endpoint và vận hành hiện tại tại [README](README.md) và [tài liệu API](API_README.md); công việc hiện tại tại [lộ trình](TODO.md); trạng thái tự động tại `.github/workflows/ci.yml`.

---

## English

The original 2025 implementation summary referenced removed modules, a planned Next.js frontend, and historical test and coverage counts. It is retained only as a short archive note so those claims are not confused with the current project state.

The active API is v2 with English JSON keys. Current setup, endpoint, and runtime documentation is in [README](README.md) and [API documentation](API_README.md); current work is in [the roadmap](TODO.md); automated status is defined by `.github/workflows/ci.yml`.
