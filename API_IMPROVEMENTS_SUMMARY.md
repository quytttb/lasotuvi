# API modernization archive

The original 2025 implementation summary referenced removed modules, a planned Next.js frontend,
and historical test/coverage counts. It is retained only as this short archive note so those claims
are not mistaken for the current project state.

The active API is v2 with English JSON keys. Current setup, endpoint and runtime documentation lives
in `README.md` and `API_README.md`; current work is tracked in `TODO.md`; automated status is defined
by `.github/workflows/ci.yml`.
